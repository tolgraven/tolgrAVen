(ns tolgraven.ui.user
  (:require
   [reagent.core :as r]
   [re-frame.core :as rf]
   ; [re-graph.core :as rg]
   ; [markdown.core :refer [md->html]]
   [clojure.string :as string]
   [tolgraven.util :as util :refer [at]]
   [tolgraven.ui :as ui]))

(defn user "User admin page" []
  [:div])

(defn register "Registration page" []
  [:div])

